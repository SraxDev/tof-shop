# Supabase — Project Settings → API
# Ces valeurs sont publiques (envoyées au navigateur), mais ne committe
# JAMAIS le .env réel : il est dans .gitignore.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Clé service_role : utilisée UNIQUEMENT par le script de conversion d'images,
# côté serveur. Ne JAMAIS l'exposer dans le frontend ni la committer.
# SUPABASE_SERVICE_KEY=
